#include <stdio.h>

void main()
{
    // Operator precedence specifies the order of operations in expressions that contain more than one operator.
    /* For example, x = 2 + 5 * 2; here, x is assigned 12, not 14 because operator * has a higher precedence than +, 
     * so it first gets multiplied with 5*2 and then adds into 2. 
    */
}